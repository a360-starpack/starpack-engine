name: streamlit
module_name: package_streamlit
entrypoint: package
version: 1.0.0
description: Packages a set of model artifacts with a FastAPI wrapper.
dataflow:
  # List of input kwargs
  input:
    - artifacts
    - metadata
    - custom_input
    - images
  output:
    - images
