name: local_docker_find
type: basic
module_name: docker_find
entrypoint: docker_find
version: 1.0.0
description: Finds already packaged model artifacts in the local Docker environment
dataflow:
  input:
    - step_data
    - package_metadata
    - images
  output:
    - images
