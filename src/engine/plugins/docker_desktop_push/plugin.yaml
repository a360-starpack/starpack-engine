name: docker_desktop_push
type: basic
module_name: docker_push
entrypoint: tag_image
version: 1.0.0
description: Tags an image and stores it in the local Docker repository.
dataflow:
  input:
    - images
    - metadata
    - step_data
